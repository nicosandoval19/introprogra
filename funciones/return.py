def suma(a, b):
    resultado = a + b
    return resultado#return es una palabra reservada que se utiliza para devolver un valor desde una función. Cuando se ejecuta una declaración return, la función termina y el valor especificado se devuelve al lugar donde se llamó a la función.

c = suma(2, 3)
d= suma(c, 4)

print(d    )